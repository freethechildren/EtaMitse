{
  "private": true,
  "scripts": {
    "start:css": "node-sass-chokidar src/ -o src/ --watch --output-style expanded",
    "start:js": "react-scripts start | cat",
    "start": "run-p start:*",
    "build:css": "node-sass-chokidar src/ -o src/ --output-style compressed",
    "build:js": "react-scripts build",
    "build": "run-s build:*"
  },
  "dependencies": {
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "materialize-css": "^1.0.0-rc.2"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.3",
    "react-scripts": "1.1.4",
    "node-sass-chokidar": "^1.3.0"
  }
}
