{
  "name": "backend",
  "private": true,
  "scripts": {
    "exec": "node src/index.js",
    "start": "npm-watch exec",
    "build": "cp -r src/ build/ && cp package.json build/"
  },
  "watch": {
    "exec": "src/**/*.js"
  },
  "dependencies": {
    "express": "^4.16.3",
    "ws": "^5.2.0"
  },
  "devDependencies": {
    "npm-watch": "^0.3.0"
  }
}
